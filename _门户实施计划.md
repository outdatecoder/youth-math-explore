# 数学学习门户 · 实施计划

> 版本 **v1.4** · 2026-09-17 · 状态：**S1–S8 已完成并通过视觉走查，仅剩 S9 发布**
> 目标：把已完成的 5 个单文件交互学习页，用一个统一门户页串成可访问、可发布的学习入口。
>
> **v1.4 变更**：①导航按钮改版 —— 左上「← 返回门户」、右下「返回顶部 ↑」（滚动 240px 后淡入），补丁 P2 升到 v2 并加旧版剥离规则；②门户图论卡片去掉章号，与其余卡片表述一致；③走查新增导航按钮行为探针（42 项断言）。
> **v1.3 变更**：①S1–S8 全部执行完毕，新增第十章「验收证据」；②新增 `.gitattributes` 行尾策略（实测源文件存在 mixed 行尾）；③修正门户配色为「每分组一个主题色」；④卡片网格改为固定列数，修复单卡分组被拉伸。
> **v1.2 变更**：①编号规则改为"单篇不带、多篇才带"；②第四章重写为「更新机制详解」，含完整实现与实测结果；③实测发现执行策略限制，新增 `_sync.cmd` 双击入口。

---

## 一、已确认决策

| # | 决策项 | 结论 |
|---|---|---|
| 1 | 跳转方式 | **整页跳转（同标签）**，浏览器后退返回门户 |
| 2 | 文件组织 | **收拢到新目录** `D:\WorkBuddy\7-数学学习门户\` |
| 3 | 收录范围 | **四学科 + 函数先导**，共 5 张卡片 |
| 4 | 发布目标 | **GitHub Pages** |
| 5 | 门户目录名 | **保持** `7-数学学习门户`（与 `1-`/`2-`/`3-`/`6-` 编号习惯一致） |
| 6 | 命名规则 | **单篇不带编号、多篇才带编号**（`graph.html`；`calculus1.html` / `calculus2.html`） |
| 7 | 返回通路 | **注入**右下角浮动"← 返回门户"按钮 |
| 8 | 微积分标注 | 卡片**标注"上篇"**，并留"下篇（规划中）"占位卡 |

---

## 二、源文件清单（已定位，无需手工提供）

| 学科 | 源路径 | 大小 | 页面标题 | 源仓库 |
|---|---|---|---|---|
| 图论 | `D:\WorkBuddy\2026-07-27-23-03-56\图论交互学习.html` | 179 KB | 第七章 万物皆可"图"——认识图论 | `2026-07-27-23-03-56` |
| 概率 | `D:\WorkBuddy\2026-08-16-18-32-38\概率交互学习.html` | 93 KB | 概率：描述可能性的数学 | `2026-08-16-18-32-38` |
| 统计学 | `D:\WorkBuddy\2026-08-16-18-32-38\统计学交互学习.html` | 143 KB | 统计学：不完美的目击证人 | `2026-08-16-18-32-38` |
| 微积分（上篇） | `D:\WorkBuddy\2026-08-16-18-32-38\微积分上篇_瞬间.html` | 496 KB | 微积分 · 上篇：瞬间（C1S1–C3S4，20 page） | `2026-08-16-18-32-38` |
| 函数先导 | `D:\WorkBuddy\2026-08-16-18-32-38\函数先导.html` | 95 KB | 函数先导：描述变化的语言 | `2026-08-16-18-32-38` |

**唯一外部依赖**：图论页第 7 行 `<script src="https://unpkg.com/cytoscape@3.28.0/dist/cytoscape.min.js">`

**源侧开发状态**：`2026-08-16-18-32-38` 仓库**正在活跃开发**（最近提交：第 3 章收官，下一步第 4 章），靠 `_build_cXsY.py` 脚本链生成 HTML。此事实决定了第四章的同步策略。

**源文件编码特征（已实测）**：均为 UTF-8 **无 BOM**；`</body>` 每页**恰好出现 1 次**（注入锚点安全）；charset 均为 UTF-8。

---

## 三、目标目录结构

```
D:\WorkBuddy\7-数学学习门户\
├── index.html                  ← 【新建】门户页
├── graph.html                  ← 图论（源→副本）
├── probability.html            ← 概率（源→副本）
├── statistics.html             ← 统计学（源→副本）
├── functions.html              ← 函数先导（源→副本）
├── calculus1.html              ← 微积分·上篇（源→副本）
│   （calculus2.html            ← 微积分·下篇，未来新增）
├── vendor\
│   └── cytoscape.min.js        ← 【新建】本地化依赖
├── _sync_from_source.ps1       ← 【已建】同步主逻辑（已实测通过）
├── _sync.cmd                   ← 【已建】双击入口（绕过执行策略限制）
├── .gitattributes              ← 【已建】行尾策略（默认 LF，.cmd 保持 CRLF）
├── .nojekyll                   ← 【已建】关闭 GitHub Pages 的 Jekyll 处理
└── .git\                       ← 【新建】独立仓库，用于发布
```

### 3.1 命名映射（中文名 → 英文 slug）

| 原文件名 | 新文件名 | 理由 |
|---|---|---|
| 图论交互学习.html | `graph.html` | 中文路径在 GitHub Pages 被 percent-encode（`/%E5%9B%BE%E8%AE%BA...`），微信分享可读性差 |
| 概率交互学习.html | `probability.html` | 同上 |
| 统计学交互学习.html | `statistics.html` | 同上 |
| 函数先导.html | `functions.html` | 同上 |
| 微积分上篇_瞬间.html | `calculus1.html` | 同上；**多篇学科带编号**，为下篇预留 `calculus2.html` |

> 页面内 `<title>` 与显示文案**保持中文不变**，只改文件名。

### 3.2 命名规则：单篇不带、多篇才带（已确认）

| 类型 | 规则 | 实例 |
|---|---|---|
| 只有一篇的学科 | `<学科>.html` | `graph.html`、`probability.html`、`statistics.html`、`functions.html` |
| 有多个分篇的学科 | `<学科><篇号>.html`，从 1 起 | `calculus1.html`（上篇）、`calculus2.html`（下篇） |

**已知代价与缓解措施**：若某学科（如图论）将来推出第二篇，`graph.html` 必须改名，**已分享出去的链接会失效**。缓解方案 —— 保留 `graph.html` 作为**重定向页**：

```html
<!DOCTYPE html><html><head><meta charset="utf-8">
<meta http-equiv="refresh" content="0; url=./graph1.html">
<link rel="canonical" href="./graph1.html"></head>
<body><a href="./graph1.html">正在跳转…</a></body></html>
```

旧链接访问 `graph.html` 会自动跳到 `graph1.html`，链接永不断。届时需新增一行即可（成本：1 个小文件）。

---

## 四、更新机制详解

> 回答两个问题：*"微积分上篇正在开发，是否复制一次到主目录，之后的更新版本是快照同步过来，不用复制操作？"* 与 *"如果是快照同步如何实现？"*

### 4.1 先纠正一个关键理解：同步本身就是复制

**"快照同步"不是一种不用复制文件的机制 —— 它内部执行的仍然是覆盖复制。**

区别只在于：**谁来做、以及顺带做不做补丁。**

| 对比项 | 初始复制（一次性） | 之后的每次同步 |
|---|---|---|
| 底层动作 | `Copy-Item` 复制文件 | **同样是覆盖复制** |
| 按映射表改名 | 手工 / 脚本 | 脚本自动 |
| 本地化 Cytoscape（补丁 P1） | 手工替换 | 脚本自动**重打** |
| 注入返回按钮（补丁 P2） | 手工注入 | 脚本自动**重打** |
| 你需要的操作 | 无（我执行 S1） | **双击一次 `_sync.cmd`** |

**所以准确的回答是**：初始复制一次 ✔；之后每次更新**仍然要复制（覆盖）**，只是由脚本自动完成 —— 省掉的是**手工操作**，不是**复制这个动作**。

**为什么补丁必须"重打"而不是"保留"？** 因为每次同步都是从**源文件**重新读取，而源文件是**干净的**（没有 CDN 替换、没有返回按钮）。上一次同步打上的补丁，会随这次覆盖而消失，所以必须重新打一遍。脚本把这四步绑在一起自动完成，这正是它存在的意义。

### 4.2 为什么不能"引用"，只能复制

| 方式 | 是否可行 | 原因 |
|---|---|---|
| 收拢复制（本方案） | ✅ | GitHub Pages 只服务一棵目录树内的文件 |
| `<a href="../源目录/x.html">` | ❌ 发布后失效 | 仓库外的文件在公网**永远无法访问** |
| 符号链接（symlink） | ❌ | git 只记录 link 条目、不跟踪目标内容，推上去指向不存在的路径，整页 404 |
| git submodule | ❌ 不推荐 | 两个源仓库**当前均无远端**；Pages 不原生装配 submodule，需引入 CI |

### 4.3 完整生命周期时间线

| 时间点 | 你的动作 | 门户目录里的文件 |
|---|---|---|
| **T0 首次搭建** | 无（我执行 S1–S3） | 5 个页面首次落位，补丁已打 |
| **T1 你在源仓库改微积分** | 正常开发：跑 `_build_c3s5.py`、git 提交（在 `2026-08-16-18-32-38` 里） | **完全不受影响，仍是旧版** |
| **T2 里程碑到了** | 双击 `_sync.cmd` | 5 个页面被**最新版覆盖**，补丁自动重打 |
| **T3 发布** | `git add . && git commit && git push`（或让我代劳） | 公网更新 |

关键：**门户目录里的 5 个 HTML 始终是"只读快照"**，你不在那里编辑任何东西。

### 4.4 两条铁律

| # | 铁律 | 违反的后果 |
|---|---|---|
| 1 | **不要在门户目录手工编辑那 5 个 HTML** | 下次同步会被源文件覆盖，你的修改**静默丢失** |
| 2 | **所有内容修改都在源仓库做** | 直接改门户副本 = 分叉，门户与源将不一致，且无从追踪 |

### 4.5 实现：两个文件

| 文件 | 作用 |
|---|---|
| `_sync_from_source.ps1` | 主逻辑：映射表 + 四步同步 + 自校验 |
| `_sync.cmd` | 双击入口：用 `-ExecutionPolicy Bypass` 调起 ps1，并 `pause` 住窗口让你看结果 |

**映射表**（脚本内，新增/删除页面只改这里）：

```powershell
$MAP = @(
    [ordered]@{ Src = 'D:\WorkBuddy\2026-07-27-23-03-56\图论交互学习.html';    Dst = 'graph.html'       },
    [ordered]@{ Src = 'D:\WorkBuddy\2026-08-16-18-32-38\概率交互学习.html';    Dst = 'probability.html' },
    [ordered]@{ Src = 'D:\WorkBuddy\2026-08-16-18-32-38\统计学交互学习.html';  Dst = 'statistics.html'  },
    [ordered]@{ Src = 'D:\WorkBuddy\2026-08-16-18-32-38\函数先导.html';        Dst = 'functions.html'   },
    [ordered]@{ Src = 'D:\WorkBuddy\2026-08-16-18-32-38\微积分上篇_瞬间.html'; Dst = 'calculus1.html'   }
)
```

### 4.6 脚本的四步动作

| 步 | 动作 | 实现要点 |
|---|---|---|
| 1 | 从源路径读入 HTML | `[System.IO.File]::ReadAllText()` —— 自动识别并剥离 BOM |
| 2 | **补丁 P1**：Cytoscape 本地化 | 正则 `<script src="https://unpkg\.com/cytoscape@[\d\.]+/dist/cytoscape\.min\.js">` → `./vendor/cytoscape.min.js`。**用正则匹配任意版本号**，源侧将来升级 Cytoscape 也不会漏替换 |
| 3 | **补丁 P2**：注入返回按钮 | 定位**最后一个 `</body>`**，在其前插入 `<style>` + `<a class="portal-back">`；带 `portal-back` 存在性判断，保证幂等 |
| 4 | 写出 + 自校验 | `WriteAllText` 以 **UTF-8 无 BOM** 写出；再逐文件校验三项：无 `unpkg.com` 残留、含 `portal-back`、以 `</html>` 收尾 |

### 4.7 已实测验证（在临时目录实跑，未触碰门户目录）

| 验证项 | 结果 |
|---|---|
| 5 个文件全部生成 | ✅ `graph / probability / statistics / functions / calculus1` 全部 `Copy=OK` |
| CDN 补丁 | ✅ `graph.html` = `REPLACED`，其余 `n/a`（本来无 CDN） |
| 按钮补丁 | ✅ 5 个文件全部 `INJECTED` |
| 无 `unpkg.com` 残留 | ✅ 5/5 `clean` |
| HTML 结构完整 | ✅ 5/5 以 `</html>` 收尾 |
| **原有 `data-page-node-id` 标记未被破坏** | ✅ 概率 408 / 统计 573 / 函数 444 / 微积分 2260 个全部保留 |
| 按钮注入位置 | ✅ 5/5 位于 `</body>` 之前 |
| **幂等性** | ✅ 连跑两次，字节数**完全一致**（180251 / 94502 / 143953 / 96093 / 497716） |
| 总自校验 | ✅ `ALL PASS` |

### 4.8 实测发现的问题：执行策略限制（已解决）

| 项 | 内容 |
|---|---|
| **现象** | 当前系统为 **Windows PowerShell 5.1 (Desktop)**，执行策略为 **Restricted**，直接运行 `.ps1` 报错：`因为在此系统上禁止运行脚本` |
| **影响** | 双击 `.ps1` 会失败 |
| **解决** | 已提供 `_sync.cmd`，内部使用 `powershell -NoProfile -ExecutionPolicy Bypass -File`，**双击即可运行**，不需要改你系统的全局策略 |
| **附带说明** | 脚本文件必须存为 **UTF-8 带 BOM**，否则 PS 5.1 会按 GBK 解析，中文源路径变乱码。当前 `_sync_from_source.ps1` 已带 BOM（实测前 3 字节 `EF BB BF`） |

### 4.9 边界情况处理

| 情况 | 行为 |
|---|---|
| 源文件被改名或移动 | 该行报 `Copy=SOURCE-MISSING`，其余 4 个仍正常同步，不会中断 |
| 新增一个页面 | 在 `$MAP` 加一行；再在门户 `index.html` 的 `CATALOG` 加一项（见 5.4） |
| 删除一个页面 | 手动删除门户目录里的对应副本（脚本**不会自动删除**，避免误删） |
| 源侧升级 Cytoscape 版本 | 正则自动匹配新版本号，无需改脚本 |
| 门户副本被手工改过 | 下次同步一律覆盖回源版本（这正是铁律 1 的由来） |

### 4.10 同步时机建议

微积分仓库**每天有多次提交**，逐次同步没有意义。建议按**里程碑**同步：

| 触发点 | 是否同步 |
|---|---|
| 源仓库出现"第 N 章收官 / 收尾 / 定稿"类提交 | ✅ 同步并推送 |
| 日常小节微调 | ❌ 跳过 |
| 准备把链接发给孩子或家长前 | ✅ 同步并推送 |

> 你的提交信息本身已带里程碑标记（如 `第 3 章收官：记忆定稿`），直接据此判断即可。

---

## 五、门户页设计规格

### 5.1 信息架构（按数学分支三组）

| 分区 | 卡片 | 一句话定位 | 前置关系 |
|---|---|---|---|
| **离散与网络** | 图论 | 万物皆可"图"——用点和线描述关系 | — |
| **不确定性与数据** | 概率 | 描述可能性的数学 | — |
| | 统计学 | 不完美的目击证人：从数据里读出真相 | 概率 → 统计学 |
| **变化的语言** | 函数先导 | 描述变化的语言 | — |
| | 微积分 · 上篇 | 抓住"一瞬间"的变化 | 函数先导 → 微积分 |
| | 微积分 · 下篇 | *（规划中占位卡，灰显不可点）* | — |

### 5.2 交互与视觉规格

| 项目 | 规格 |
|---|---|
| 跳转 | 整页跳转 `location.href='./graph.html'`，同标签 |
| 卡片可点区域 | 整张卡片可点（非仅按钮） |
| 触摸目标 | 按钮/卡片高度 ≥ 50 px（沿用项目儿童向规范） |
| 配色 | 沿用现有 6 色 CSS 变量体系；**每分组一个主题色**（组内统一，强化"这几个是一类"的认知） |
| 布局 | 桌面 3 列 / 平板 2 列（≤920px）/ 手机 1 列（≤600px）。**固定列数 `repeat(3,1fr)`，不用 `auto-fit`** —— 否则只有 1 张卡的分组会被拉伸到整行 |
| 顶部 | 门户标题 + 一句话说明（"给孩子的数学探索台"） |
| 离线可用 | 全本地资源，无 CDN 请求（Cytoscape 已本地化） |
| 占位卡 | `ready:false` 的卡片灰显 + "规划中"角标，点击无响应 |

### 5.3 返回通路（已确认注入，v2 布局）

门户 → 学科页为同标签跳转。为降低孩子迷路概率，给 5 个副本页各注入两枚浮动按钮（由同步脚本的补丁 P2 完成，无需手工）：

| 位置 | 按钮 | 行为 |
|---|---|---|
| **左上角** | `← 返回门户` | 固定显示，链接到 `./index.html` |
| **右下角** | `返回顶部 ↑` | 滚动超过 240 px 才淡入；点击平滑回到顶部 |

```html
<a href="./index.html" class="portal-back" aria-label="返回门户">← 返回门户</a>
<button type="button" class="portal-top" id="portal-top-btn" aria-label="返回顶部">返回顶部 ↑</button>
```

补丁块用 `<!-- portal-nav:start -->` … `<!-- portal-nav:end -->` 包裹，脚本每次同步会先剥离旧块再注入，因此改版不会残留旧按钮（脚本同时保留 v1 无标记补丁的剥离规则，用于覆盖旧副本）。

样式要点：`position:fixed`、`z-index:99999`、48 px 高（手机 44 px）、半透明深色胶囊底 + 白字。隐藏态用 `opacity:0 + visibility:hidden + pointer-events:none` 三合一 —— 仅用 `opacity:0` 会让按钮留在 tab 顺序与无障碍树里。

**为什么"返回顶部"要按需出现**：5 个页面的首页高度差异很大。实测桌面端（1280×900）可滚动余量：图论 1089 px、概率 507 px、微积分 800 px，而统计 170 px、函数 157 px。后两页几乎不需要回顶，阈值 240 px 正好把它们自动排除掉，不必逐页配置。

**验证要点**：`返回门户` 在左上角且不压到各页原有顶部元素（图论页的 `CHAPTER 7` 是居中排布，不冲突）；`返回顶部` 在右下角且不遮挡正文。走查截图见 `shots/`。

### 5.4 数据驱动结构（扩展性基础）

卡片**不写死在 HTML 里**，由配置数组渲染：

```js
const CATALOG = [
  { group: '离散与网络', accent: '--c-blue', items: [
    { file: 'graph.html', title: '图论', sub: '万物皆可"图"',
      desc: '用点和线描述关系', badge: '1 篇', ready: true },
  ]},
  { group: '不确定性与数据', accent: '--c-amber', items: [
    { file: 'probability.html', title: '概率', sub: '描述可能性的数学', ready: true },
    { file: 'statistics.html',  title: '统计学', sub: '不完美的目击证人', ready: true },
  ]},
  { group: '变化的语言', accent: '--c-violet', items: [
    { file: 'functions.html',  title: '函数先导', sub: '描述变化的语言', ready: true },
    { file: 'calculus1.html',  title: '微积分 · 上篇', sub: '瞬间',
      desc: '抓住"一瞬间"的变化', badge: '20 page', ready: true },
    { file: 'calculus2.html',  title: '微积分 · 下篇', sub: '规划中', ready: false },
  ]},
];
```

**预留字段**：`ready`（可点/占位）、`badge`（篇数/状态角标）、`accent`（主题色）、`desc`（副标题）、`series`（内容来源系列，预留）。

**扩展成本表**：

| 扩展场景 | 需要改动 | 成本 |
|---|---|---|
| 微积分下篇完成 | 把 `calculus2` 的 `ready:false` 改成 `true` | **改 1 个字段** |
| 新增一篇（如图论第八章） | 门户放 `graph1.html` + `CATALOG` 加一项 + `$MAP` 加一行 + `graph.html` 改重定向页 | 小 |
| 新增一个学科 | `CATALOG` 加一项 + `$MAP` 加一行 | **加 2 行** |
| 新增一个分组（如"代数"） | `CATALOG` 加一组 | 加 5 行 |
| 页面数超过 ~12 篇 | 需要加筛选/搜索 | 中等（届时再评估） |

**结论**：扩展性**良好**。前四类都在"改配置、不改逻辑"的范围，不碰 CSS 也不碰渲染函数。

---

## 六、实施步骤

| 步骤 | 动作 | 验收条件 | 状态 |
|---|---|---|---|
| **S1** | 复制 5 个源文件到门户目录并按 3.1 表重命名 | 5 个文件存在，字节数正常 | ✅ |
| **S2** | 下载 `cytoscape@3.28.0` 到 `vendor/`，替换 `graph.html` 的 CDN 引用 | 断网打开 `graph.html`，可正常渲染图 | ✅ |
| **S3** | 为 5 个副本页注入浮动"返回门户"（左上）+ "返回顶部"（右下）按钮 | 左上出现返回门户，右下滚动后出现返回顶部，均不遮挡正文 | ✅ |
| **S4** | 新建 `index.html`（`CATALOG` 数据驱动 + 三组卡片 + 响应式） | 6 张卡（含 1 占位）显示正确、跳转正确、三档布局正常 | ✅ |
| **S5** | 添加 `.nojekyll` | 文件存在 | ✅ |
| **S6** | 编写同步脚本 `_sync_from_source.ps1` + `_sync.cmd` | 脚本输出 `ALL PASS`；重复运行结果一致 | ✅ |
| **S7** | `git init` + 首次提交（身份 `Pengc <pengc@local>`，推送前可改真实邮箱） | 提交成功，`git log` 可见 | ✅ |
| **S8** | 本地起 `python -m http.server 8765` 走查 | 逐页 200、渲染正确、无遮挡 | ✅ |
| **S9** | 发布到 GitHub Pages（见第七章） | 公网 URL 可访问，微信内可打开 | ⬜ **待你提供仓库信息** |

> S1–S3 实际由 S6 的脚本一次完成（首次执行 = 双击一次 `_sync.cmd`）。

---

## 七、GitHub Pages 发布路径

### 7.1 当前阻塞点（需你参与）

| 阻塞项 | 现状 | 需要做什么 |
|---|---|---|
| `gh` CLI | **未安装** | 改用网页建仓 + git 推送，或安装 gh |
| 远端仓库 | 两个源仓库**均无 remote** | 需新建一个 GitHub 仓库（建议名 `math-explore`，public） |
| 推送凭据 | 未配置 | 生成 **Personal Access Token**（或配置 SSH key），首次 `push` 时输入 |
| git 邮箱 | `pengc@local` | 建议推送前改为你的 GitHub 邮箱，否则提交不关联账号 |

### 7.2 发布步骤（S9 展开）

1. 在 GitHub 网页新建 **public** 仓库 `math-explore`（**不要**勾选 Add README / .gitignore）；
2. 本地配置身份与远端：
   ```
   git config user.email "<你的 GitHub 邮箱>"
   git remote add origin https://github.com/<用户名>/math-explore.git
   ```
3. 推送：`git push -u origin main`（首次要求输入用户名 + PAT）；
4. 仓库 **Settings → Pages → Source** 选 `Deploy from a branch`，分支 `main`、目录 `/ (root)`，保存；
5. 等待约 1 分钟，访问 `https://<用户名>.github.io/math-explore/`。

### 7.3 GitHub Pages 特有约束与对策

| 约束 | 影响 | 对策 |
|---|---|---|
| 文件系统**大小写敏感**（Linux） | Windows 本地不敏感，线上会 404 | 文件名统一小写，引用路径严格匹配 |
| Jekyll 默认处理 | 忽略 `_` 前缀文件 | 放置 `.nojekyll`；`_sync_*` / `_计划*.md` 也以 `_` 开头，天然不参与发布 |
| 中文 URL 编码 | 中文文件名路径变 `%E5%9B%BE...` | 已通过英文 slug 规避（3.1） |
| HTTPS 环境 | 混合内容被拦截 | 本地化 Cytoscape 后无 HTTP 外链 |
| CDN 可达性 | unpkg 在国内不稳定 | 本地化后彻底消除（S2） |
| 仓库大小 | 6 页共约 1 MB，远低于 1 GB 限制 | 无风险 |

---

## 八、风险登记

| 级别 | 风险 | 对策 |
|---|---|---|
| **P0** | 副本与源仓库**分叉** | 已由第四章同步机制覆盖：脚本幂等 + 自校验 + 里程碑同步 |
| **P1** | 图论页 CDN 引用被替换后，若 `vendor/cytoscape.min.js` 缺失则整页失效 | S2 验收含"断网可用"；脚本第 4 步校验 `unpkg.com` 残留 |
| **P1** | 注入按钮遮挡原页右下角元素 | 注入后逐页走查；按钮 z-index 99999 且靠右下 16 px，必要时按页微调偏移 |
| **P2** | 执行策略 Restricted 导致脚本无法运行 | 已提供 `_sync.cmd` 绕过（见 4.8） |
| **P2** | `pengc@local` 邮箱导致提交不关联 GitHub 账号 | 推送前改邮箱（见 7.1） |
| **P2** | 忘了同步，门户显示旧内容 | 发布前固定跑一次 `_sync.cmd`，输出 `ALL PASS` 即确认 |
| **P2** | 单篇学科将来拆篇导致 `graph.html` 链接失效 | 保留重定向页方案（见 3.2） |

---

## 九、待确认清单

| # | 事项 | 结论 / 建议 | 状态 |
|---|---|---|---|
| 1 | 门户目录名 `7-数学学习门户` | 保持 | ✅ 已确认 |
| 2 | 文件名改英文 slug | 执行 | ✅ 已确认 |
| 3 | 微积分命名 `calculus1` / `calculus2` | 执行 | ✅ 已确认 |
| 4 | 是否注入浮动"返回门户"按钮 | 注入：左上「返回门户」+ 右下「返回顶部」 | ✅ 已确认（v1.4 调整位置） |
| 5 | 微积分卡片标注"上篇" | 标注 | ✅ 已确认 |
| 6 | 命名规则 | **单篇不带、多篇才带** | ✅ 已确认 |
| 7 | 同步机制实现 | 快照同步脚本 | ✅ 已完成并实测 |
| 8 | GitHub 仓库名 / 用户名 | 待提供（S9 时使用） | ⬜ 待提供 |
| 9 | 是否现在安装 `gh` CLI 简化发布 | 可选，不装也能发 | ⬜ 待定 |
| 10 | 图论页内页自带 `CHAPTER 7` / "第七章" 标注 | 门户已隐去章号；源页面（`2026-07-27-23-03-56`）内的标注是否要一并改，待你确认 —— 该页有 s1–s6 共 6 节 | ⬜ 待确认 |

---

## 十、验收证据（S8 走查实测 · v1.4 复核）

### 10.1 HTTP 层

| 路径 | 状态码 | 字节数 |
|---|---|---|
| `/` 与 `/index.html` | 200 | 11,000+ |
| `/graph.html` | 200 | 177,444 |
| `/probability.html` | 200 | 95,626 |
| `/statistics.html` | 200 | 143,349 |
| `/functions.html` | 200 | 97,217 |
| `/calculus1.html` | 200 | 498,840 |
| `/vendor/cytoscape.min.js` | 200 | 365,703 |

### 10.2 内容层

| 检查项 | 结果 |
|---|---|
| 5 页 `unpkg.com` 残留 | **0**（全部本地化） |
| 5 页各含 1 个 `portal-back` 链接 + 1 枚 `portal-top-btn` | ✅ |
| 5 页各含 1 个 `<!-- portal-nav:start -->` 标记 | ✅ 无重复注入 |
| v1 遗留补丁 `<style id="portal-back-style">` | ✅ 0（改版无残骸） |
| 原有 `data-page-node-id` 标记 | ✅ 概率 408 / 统计 573 / 函数 444 / 微积分 2260 全部保留 |
| 同步幂等性 | ✅ 连跑两次结果一致，`ALL PASS` |
| 临时文件残留 | ✅ none |

### 10.3 渲染层（Edge headless 视觉走查）

| 页面 | 结果 |
|---|---|
| 门户 · 桌面 1280 | ✅ 三组分区、6 张卡、前置提示、占位卡全部正确渲染 |
| 门户 · 平板 834 | ✅ 2 列布局正确 |
| 门户 · 手机 400 | ✅ 1 列布局正确 |
| 图论卡片副标题 | ✅ 显示「万物皆可「图」」，无章号，与其余卡片一致 |
| 图论页 · 顶部 | ✅ 左上「返回门户」可见，不压 `CHAPTER 7`；右下「返回顶部」隐藏 |
| 5 学科页 · 强制显示 | ✅ 两枚按钮位置、配色、不遮挡正文均正常 |

**Cytoscape 本地化硬验证**：探针执行后 `typeof window.cytoscape === 'function'`、`version === '3.28.0'` —— 证明图由**本地 vendor 脚本**提供，而非 CDN。

### 10.4 导航按钮行为探针（42 项断言 ALL PASS）

截图无法可靠验证 fixed 元素 + 程序化滚动的组合，故显隐行为改用 DOM 探针（读 `getComputedStyle` / `getBoundingClientRect`，并给 `window.scrollTo` 打桩）。逐页断言：

| 断言 | 结果 |
|---|---|
| 返回门户 `left/top = 16/16`、高 48 px、`href=./index.html` | ✅ 5/5 |
| 返回顶部右/下边缘贴视口 16 px（滚动前后均成立） | ✅ 5/5 |
| 未滚动时返回顶部 `opacity=0` + `visibility=hidden` | ✅ 5/5 |
| 滚动到底后返回顶部 `opacity=1` + `visibility=visible` | ✅ 3/3 可滚动页 |
| 点击后 `scrollTo({top:0,behavior:'smooth'})` 且落回 y=0 | ✅ 3/3 可滚动页 |
| 不可滚动页（统计 maxY=170 / 函数 157）保持隐藏 | ✅ 正确行为 |

**两个必须记录的 headless 陷阱**（已写进 `_shot.py` 注释）：

1. `--virtual-time-budget` 会**冻结 CSS transition**，直接读 `opacity` 拿到的是起始值（1 而非 0），会误判成"隐藏失效"。探针页因此额外注入 `transition:none`。这只影响淡入动画本身，不影响被验证的"滚动位置 → class 切换"逻辑。
2. `window.innerWidth` **含滚动条宽度**，右对齐断言必须用 `document.documentElement.clientWidth`，否则会稳定误报 16 px 偏差。

### 10.5 git 层

```
e80e185  feat(nav): 返回门户移至左上角，右下角改为返回顶部；门户卡片去掉图论章号
5257bdf  docs: 计划升至 v1.3 + 新增 .gitignore 与截图脚本
fd56731  fix(portal): 卡片网格改为固定列数，修复单卡分组被拉伸到整行
d7622de  init: 数学学习门户 v1.0 —— 门户页 + 5 个学习页快照
```

工作区干净；行尾检查：`_sync.cmd` = `w/crlf`，其余全部 `w/lf`。


### 10.5 本轮新发现并解决的三个坑

| 坑 | 现象 | 解决 |
|---|---|---|
| **行尾噪音** | `graph.html` / `statistics.html` 是 **mixed 行尾**（CRLF + LF 混用）。Windows 的 `core.autocrlf=true` 会在每次触碰时改写行尾，导致 `git status` 反复报"已修改"、真实 diff 被行尾噪音淹没 | 两层治理：①`.gitattributes` 定 `* text=auto eol=lf`，`.cmd` / `.bat` 豁免为 CRLF（LF 会让 cmd.exe 解析异常）；②同步脚本第 4 步把 `\r\n` / `\r` 统一为 `\n`，与 git 索引对齐 |
| **执行策略 Restricted** | 本机直接运行 `.ps1` 报"禁止运行脚本" | `_sync.cmd` 用 `-ExecutionPolicy Bypass` 调起，不改系统全局策略 |
| **PS 5.1 中文编码** | 脚本含中文源路径，若无 BOM 会按 GBK 解析导致路径乱码 | 脚本存为 **UTF-8 带 BOM**（实测 `EF BB BF`） |

---

## 十一、下一步

| # | 事项 | 需要谁 |
|---|---|---|
| 1 | 提供 GitHub 仓库名 + 用户名 | 你 |
| 2 | 新建 public 仓库、生成 PAT | 你 |
| 3 | 配置 remote + push + 开启 Pages | 我（拿到信息后） |
| 4 | 可选：安装 `gh` CLI 简化后续发布 | 你决定 |

**日常使用**：源仓库改完 → 双击 `_sync.cmd` → `git push`。
