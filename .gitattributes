# 统一行尾策略
# ------------------------------------------------------------------
# 默认全部使用 LF：
#   1) 与源文件一致（源 HTML 本身就是 LF，同步脚本原样保留行尾）
#   2) 适配 GitHub Pages 的 Linux 环境，避免 checkout 时被改写
#   3) 避免每次 git 操作产生无意义的行尾 diff
* text=auto eol=lf

# Windows 批处理必须是 CRLF，LF 行尾可能让 cmd.exe 解析异常
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制资源不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
